<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demo">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".event_details"
            android:parentActivityName=".Events"
            android:label="Event_Details"/>
        <activity android:name=".reg_page" />
        <activity android:name=".homepage" />
        <activity android:name=".Edit_Marks" />
        <activity android:name=".leave_application" />
        <activity android:name=".placement_filter" />
        <activity android:name=".event_filter" />
        <activity android:name=".View_atn" />
        <activity android:name=".Events" />
        <activity android:name=".Placements" />
        <activity android:name=".Examination" />
        <activity android:name=".Attendance" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>